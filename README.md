# Python-Discord-Bot

## Setup

1. Copy the `.env` template ```bash cp .env.template .env```
2. Login to the Discord developer portal.
3. Overview > Bot
4. Generate a new token by pressing 'Reset Token'
5. Copy the token and past into the `.env` file as `DISCORD_TOKEN`
6. 
